name: test

on:
  push:
    branches:
      - '*'
    tags-ignore:
      - '*'
  pull_request:

jobs:
  raku:
    strategy:
      matrix:
        os:
          - windows-latest
        raku-version:
          - 'latest'
          - '2023.08'
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
      - uses: Raku/setup-raku@v1
        with:
          raku-version: ${{ matrix.raku-version }}
      - name: Install Dependencies
        run: |
          raku -e'say q<distro:> ~ $*DISTRO.name'
          zef install --/test App::Prove6
          zef install --deps-only .
          zef build .
      - name: Run Tests
        run: prove6 -I. t
